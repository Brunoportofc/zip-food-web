rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regra temporária para debug - permitir tudo
    match /{document=**} {
      allow read, write: if true;
    }
  }
}